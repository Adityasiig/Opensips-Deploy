# Opensips-Deploy

Local, Docker-packaged deployment UI for OpenSIPS.
Ships a bundled config snapshot so no "source server" is required at deploy time.

Setup and usage docs arrive in the following commits.
